# Space Syntax解析システム - Python依存関係
# 実行前に pip install -r requirements.txt でインストールしてください

# 必須依存関係 (Core Dependencies)
networkx>=2.5
osmnx>=1.0.0
geopandas>=0.10.0
pandas>=1.4.0
numpy>=1.22.0
scipy>=1.8.0
shapely>=2.0.0

# 可視化関係 (Visualization)
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.0.0

# レポート生成 (Report Generation)
jinja2>=3.0.0

# オプション依存関係 (Optional Dependencies)
# インタラクティブマップ用
folium>=0.12.0

# PDF生成用 (いずれか一つをインストール)
weasyprint>=54.0  # 推奨
# pdfkit>=1.0.0   # 代替案 (wkhtmltopdfが必要)

# パフォーマンス監視用
psutil>=5.8.0

# 開発・テスト用 (Development & Testing)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950

# Jupyter notebook用 (オプション)
jupyter>=1.0.0
ipykernel>=6.0.0

# 追加の地理空間ライブラリ (オプション)
rasterio>=1.3.0
contextily>=1.2.0  # 背景地図用

# プロファイリング用 (オプション)
memory_profiler>=0.60.0
line_profiler>=3.5.0

# その他のユーティリティ
tqdm>=4.60.0  # プログレスバー
rich>=12.0.0  # リッチテキスト表示
click>=8.0.0  # CLI拡張 (将来用)

# 特定バージョン固定 (安定性のため)
requests>=2.27.0,<3.0.0
urllib3>=1.26.0,<2.0.0

# 日本語フォント対応 (オプション)
# japanize-matplotlib>=1.1.0  # 日本語フォント自動設定

# プラットフォーム固有 (コメントアウト状態)
# Windows
# pywin32>=227; sys_platform == "win32"

# macOS  
# pyobjc-framework-Cocoa>=8.0; sys_platform == "darwin"

# Linux
# python3-tk; sys_platform == "linux"  # tkinter GUI用
